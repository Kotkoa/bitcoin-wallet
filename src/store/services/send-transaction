export const sendTransaction = async (hex: string): Promise<string> => {
  const response = await fetch('https://blockstream.info/testnet/api/tx', {
    method: 'POST',
    body: hex,
    headers: {
      'Content-Type': 'text/plain',
    },
  });
  return response.text();
};
